const BACKEND_PORT = 8082;
const BACKEND_HOST = "localhost";

export const API_CONFIG = {
  BASE_URL: `http://${BACKEND_HOST}:${BACKEND_PORT}/api`,
  BACKEND_PORT,
  BACKEND_HOST,
};

export default API_CONFIG;
